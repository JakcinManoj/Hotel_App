const assert = require('assert');
const { describe } = require('mocha');
const { it } = require('mocha');

describe('Check whether the dom is loaded or not', function(){
    it('DOM is Loaded', function(){
        assert(true);
    });
})