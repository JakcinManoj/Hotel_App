{
  "name": "hotel-app",
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "start": "lerna run start --stream",
    "format": "lerna run format --stream",
    "build": "lerna run build",
    "deploy": "yarn bootstrap && yarn build",
    "preserve": "yarn build",
    "postinstall": "lerna bootstrap",
    "test": "cd hotel-frontend/test && yarn mocha",
    "cypress": "cypress run --spec '**/*.cy.js'"
  },
  "devDependencies": {
    "cypress": "11",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "cypress-xpath": "^2.0.1",
    "lerna": "^3.22.1",
    "mocha": "9.1.3"
  }
}
